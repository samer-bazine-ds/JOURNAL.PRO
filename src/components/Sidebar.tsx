import { FileText, Search, Settings, Shuffle, LogOut, RefreshCw, Scale } from 'lucide-react'; import type { Page } from '../types';
const links: {id:Page;label:string;Icon:typeof FileText}[]=[{id:'import',label:'Importer PDF',Icon:FileText},{id:'review',label:'Réviser & Exporter',Icon:Search},{id:'mapping',label:'Correspondances',Icon:Shuffle},{id:'settings',label:'Plan comptable',Icon:Settings}];
export function Sidebar({page,count,onNavigate,onModules,onLogout}:{page:Page;count:number;onNavigate:(p:Page)=>void;onModules:()=>void;onLogout:()=>void}) { return <nav className="sidebar"><div className="sidebar-logo"><div className="logo-icon"><Scale/></div><div className="logo-text"><span className="logo-title">JournalPro</span><span className="logo-sub">Comptabilité · Ventes</span></div></div><ul className="nav-links">{links.map(({id,label,Icon})=><li key={id}><button className={`nav-link ${page===id?'active':''}`} onClick={()=>onNavigate(id)}><Icon/><span>{label}</span>{id==='review'&&count>0&&<span className="nav-badge">{count}</span>}</button></li>)}</ul><div className="sidebar-footer"><button className="btn btn-outline full" onClick={onModules}><RefreshCw/> Changer de module</button><button className="btn btn-outline full" onClick={onLogout}><LogOut/> Changer de compte</button><small>v2.0 · Electron</small></div></nav> }
